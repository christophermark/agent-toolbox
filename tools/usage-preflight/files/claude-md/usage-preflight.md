## Usage-limit preflight

Before work likely to consume substantial model usage — multiple subagents, a
comprehensive security review or workflow, broad repository-wide analysis, an
extended autonomous implementation, or retrying a large workflow — invoke the
`usage-preflight` skill and follow it.
